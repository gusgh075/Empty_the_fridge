server:
  port: 8888
spring:
  application:
    name: config-server
  profiles:
    active: local
  cloud:
    config:
      server:
        git:
          uri: https://github.com/gusgh075/config-server-test.git
